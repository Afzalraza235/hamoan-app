import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.portalhamoan.hamoan',
  appName: 'Hamoan',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
